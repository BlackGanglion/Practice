#include <cstdio>
#include <algorithm>
#include <string>
#include <cstring>
using namespace std;

class Solution {
public:
    string convert(string s, int numRows) {
    	string ans;
    	vector< vector<int> > v;
    	

        return ans;
    }
};

int main() {
	string input = "PAHNAPLSIIGYIR";
	int numRows = 3;

	Solution sol;
	sting ans = sol.convert(input, numRows);
	cout << ans << endl;
}